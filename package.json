{
  "name": "server",
  "version": "1.0.0",
  "main": "./server.js",
  "license": "MIT",
  "scripts": {
    "serve:dev": "json-server --watch db.json --port 9001",
    "serve:build": "json-server db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
